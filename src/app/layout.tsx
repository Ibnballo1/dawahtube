import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
// import Navbar from "@/components/layout/Header";
// import Footer from "@/components/layout/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Da'waTube - Your Source to Islamic Knowledge",
  description:
    "Da'waTube is your go-to platform for authentic Islamic knowledge, featuring articles, videos, and resources on various aspects of Islam.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {/* <Navbar /> */}
        <main className="min-h-[calc(100vh-160px)] bg-white">{children}</main>
        {/* <Footer /> */}
      </body>
    </html>
  );
}
